   apiVersion: v1
   kind: PersistentVolumeClaim
   metadata:
     name: fl-pvc-cblee
     namespace: cblee-credo  # Your specific namespace
   spec:
     accessModes:
       - ReadWriteOnce
     resources:
       requests:
         storage: 10Gi  # Adjust size as needed
